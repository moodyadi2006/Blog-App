function Container({children}) {
  return (
    <div className="w-full max-w-7xl mx-auto px-4">
      {children}
    </div>
  )
}

//Container is just a box in which we specify styling properties

export default Container;